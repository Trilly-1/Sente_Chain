# Baked into Vite production builds (Vercel, etc.)
# Local dev still uses .env with localhost — see .env.example

VITE_API_URL=https://sente-chain.onrender.com
VITE_USE_DEMO=false
VITE_SKIP_KYC=true
VITE_STELLAR_NETWORK=testnet
